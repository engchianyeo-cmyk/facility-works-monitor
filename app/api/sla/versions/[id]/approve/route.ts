import{NextResponse}from"next/server";import{getCurrentIdentity}from"@/lib/auth";import{createClient}from"@/lib/supabase/server";export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const i=await getCurrentIdentity();if(!i)return NextResponse.json({ok:false},{status:401});if(!["supervisor","administrator"].includes(i.role))return NextResponse.json({ok:false},{status:403});const{id}=await params;let note="";try{note=String(((await request.json())as{note?:unknown}).note??"")}catch{}const{data,error}=await(await createClient()).rpc("approve_sla_version",{p_version_id:id,p_note:note||null});return error?NextResponse.json({ok:false},{status:500}):NextResponse.json(data,{status:(data as{ok?:boolean})?.ok?200:400});}
