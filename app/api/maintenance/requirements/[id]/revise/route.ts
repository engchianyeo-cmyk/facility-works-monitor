import { getCurrentIdentity } from "@/lib/auth"; import { pmRpcResponse, pmTransportFailure } from "@/lib/maintenance/api"; import { approvedPmPayload, canManagePm, type PmRpcResult } from "@/lib/maintenance/types"; import { createClient } from "@/lib/supabase/server"; import { errorResponse } from "@/lib/work-orders/api";
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { const identity=await getCurrentIdentity();if(!identity)return errorResponse("AUTHENTICATION_REQUIRED","Authentication is required.",401);if(!canManagePm(identity.role))return errorResponse("ACCESS_DENIED","Supervisor or Administrator authority is required.",403);let body:Record<string,unknown>;try{body=await request.json() as Record<string,unknown>}catch{return errorResponse("VALIDATION_ERROR","Request body must be valid JSON.")};try{const{id}=await params;const supabase=await createClient();const{data,error}=await supabase.rpc("revise_pm_requirement",{p_requirement_id:id,p_payload:approvedPmPayload(body),p_reason:body.revision_reason});if(error)return pmTransportFailure("revise");return pmRpcResponse(data as PmRpcResult|null)}catch{return pmTransportFailure("revise")}}
