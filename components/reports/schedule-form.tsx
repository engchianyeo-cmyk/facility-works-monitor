"use client";import{useState}from"react";export function ScheduleForm(){const[m,setM]=useState("");async function submit(f:FormData){const r=await fetch("/api/report-schedules",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:String(f.get("name")),cadence:String(f.get("cadence")),scope:{},recipient_roles:["administrator"],next_run_at:new Date().toISOString()})});const j=await r.json()as{ok?:boolean};setM(j.ok?"Schedule created. Delivery: NOT_CONFIGURED":"Schedule could not be created.");}return <form action={submit} className="grid gap-3 rounded-xl border bg-white p-5 sm:grid-cols-3"><label className="text-sm font-bold">Schedule name<input name="name" required minLength={3} className="mt-1 min-h-11 w-full rounded border p-2"/></label><label className="text-sm font-bold">Cadence<select name="cadence" className="mt-1 min-h-11 w-full rounded border p-2"><option value="daily">Daily</option><option value="weekly">Weekly</option><option value="monthly">Monthly</option></select></label><button className="min-h-11 self-end rounded bg-slate-900 px-4 font-bold text-white">Create schedule</button>{m&&<p role="status" className="sm:col-span-3">{m}</p>}</form>}
